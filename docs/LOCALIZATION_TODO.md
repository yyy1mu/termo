# macOS 本地化整改 TODO

目标：`跟随系统 / 简体中文 / English` 在运行中切换后，主窗口、设置页、二级弹窗、动态状态和错误提示立即使用同一种语言。历史聊天、远端命令输出、主机名和用户输入保持原文。

## P0：语言状态与传播

- [x] 新安装默认跟随 macOS 全局语言顺序。
- [x] 清理旧版写入的应用级 `AppleLanguages`，不再用私有偏好覆盖系统语言。
- [x] SwiftUI 根视图直接观察 `AppSettings.appLanguage`，避免从 `UserDefaults` 回读旧值。
- [x] 在 `@Published` 发出变化前持久化语言，避免普通 `String` 短暂使用切换前的资源包。
- [x] 设置页在自身视图树重新注入语言，并把语言环境放在 sheet/alert 外层。
- [x] 根窗口把语言环境放在锁屏 overlay 外层，锁屏不再回退到系统语言。
- [x] 应用菜单、独立“关于”窗口和终端右键菜单跟随应用内语言切换。
- [x] 同步数据缺少或包含非法语言值时回退到 `system`。

## P1：设置页与二级弹窗

- [x] 通用、终端、传输、资源告警、快捷键、关于页面。
- [x] AI 设置、同步与备份、安全、SSH 密钥列表。
- [x] “生成 SSH 密钥”显式观察应用语言，不依赖 sheet 创建时捕获的环境。
- [x] 密钥详情与部署公钥弹窗使用同一语言传播规则。
- [x] 密钥弹窗使用的 49 个静态文案均有独立英文翻译，并覆盖标题、字段、占位文本、按钮、帮助和辅助功能标签。
- [x] 钥匙串读取、保存、恢复及私钥读写错误使用完整句子翻译，避免英文界面夹杂中文操作词。
- [x] 密钥生成 FFI 的 6 类中文失败信息在 Swift 边界转换为当前界面语言。
- [ ] 用中英文各走一遍设置页内全部二级弹窗，检查标题、字段、占位文本、按钮、帮助和错误状态。

## P2：主机与工作区

- [ ] 主机侧栏、会话侧栏、标签栏和空状态。
- [ ] 新建/编辑主机的左右导航、认证、代理、终端和高级设置。
- [ ] 连接测试、密码请求、主机指纹确认和删除确认。
- [ ] 主机概览、监控、文件、tmux、端口转发及右侧运维面板。

## P3：动态字符串

- [x] 所有 `String(localized:)` 绑定应用选择的 bundle 和 locale。
- [x] SSH 编码及算法选项从冻结的 `static let` 改为按当前语言重新生成。
- [x] AI 配置、压缩、工具调用、长度限制和终端截取错误改为可本地化字符串。
- [ ] 审核缓存于 ObservableObject 的状态字符串；语言切换后仍有效的状态必须重新计算。
- [ ] 审核返回 `String` 的枚举和格式化函数，区分界面文案与用户/远端数据。
- [ ] 将 `TermoSSH` 返回的中文错误改为稳定错误码，并在 Swift UI 边界本地化；连接、转发、SFTP、扫描及密钥失败目前仍可能把 Rust 中文错误详情带入英文界面。

## P4：String Catalog

- [x] `zh-Hans` 和 `en` 均覆盖当前目录条目。
- [x] 静态扫描 1277 处界面文案引用，确认没有静态 key 遗漏目录，且英文译文不残留中文字符。
- [x] 补充本次发现的 AI 动态错误和终端截取提示。
- [ ] 编译后重新提取活动 key，删除失效项并确认没有仅存在于源码、目录未收录的文案。
- [ ] 人工复核英文长度、术语一致性和复数格式，避免只做逐字翻译。

## P5：自动检查与验收

- [x] 增加本地化审计脚本：检查缺失翻译、绕过应用 bundle/locale、中文 `verbatim` 和冻结的本地化静态值。
- [x] 增加语言切换回归测试，覆盖系统语言、显式中文、显式英文、密钥弹窗文案和旧偏好迁移。
- [ ] English：主窗口 → 设置 → 生成密钥 → 密钥详情 → 部署公钥完整走查。
- [ ] 简体中文：重复同一路径，确认无英文回退。
- [x] Release 编译与 `git diff --check`。

## 当前审计记录（2026-09-26）

- String Catalog 当前有 1782 个条目；`en` 缺失 0，`zh-Hans` 缺失 0。
- 源码静态检查覆盖 1277 处 `Text/Button/Label/Toggle/Picker/alert/help/String(localized:)` 等界面文案引用，目录缺失 0。
- 已确认“生成 SSH 密钥”的英文翻译存在；故障来自嵌套 sheet 的隐式解析。密钥生成、详情和部署页现已显式从应用选择的 bundle 取值。
- 已检查 Release 成品的 `en.lproj` 与 `zh-Hans.lproj`：密钥标题、字段、口令说明及钥匙串错误均按语言写入资源文件。
- 已发现并修复 4 组冻结的 SSH 选项列表、12 处 AI/终端动态中文提示、2 处部署公钥错误未指定应用资源包。
- 已确认旧式 `extractLocStrings` 无法正确解析项目中带变量的 SwiftUI 文案，不能把其 27 个 `%@` 结果当作真实缺失项；需以 String Catalog 编译、静态审计和运行态走查交叉验收。
- LLM system prompt、SSH/终端输出、主机名、路径和用户内容属于业务数据，不随界面语言自动翻译。
