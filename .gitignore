.build/
.swiftpm/
.DS_Store
termo.app/
Termo.app/
.spec-workflow/

# Xcode：生成的 .xcodeproj 本身提交（XcodeGen 输出确定性，diff 稳定），仅忽略个人/构建状态
xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
DerivedData/
.xcbuild/
dist/

# FreeRDP 构建中间产物（见 scripts/build-freerdp.sh；产物 Vendor/CFreeRDP.xcframework 可选提交）
.freerdp-build/

# libssh2 静态库构建中间产物（SSH 迁移 J1）
.libssh2-build/

# MCP 工具运行日志
.playwright-mcp/

# Sparkle 发布期工具（generate_keys/sign_update/generate_appcast）：仅本地一次性用，CI 自下载，不入库
scripts/sparkle-tools/

# 私人发布手册等文档：仅本地查阅，不入库
docs/

# russh/构建产物（scripts/build-russh.sh 输出；只保留未跟踪，不入库）
/Build/

# Rust 构建产物（cargo 默认 target 目录）
Rust/**/target/
